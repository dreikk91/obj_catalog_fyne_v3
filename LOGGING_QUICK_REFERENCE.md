# 🔍 Швидкий довідник логування

## Основні етапи запуску з логами

```
ЗАПУСК
├─ 📋 Логер ініціалізується (logger.go)
├─ 🏠 Fyne додаток створюється (main.go)
├─ ⚙️  Налаштування БД завантажуються (db_config.go)
├─ 📂 З'єднання з Firebird встановлюється (connect.go)
├─ 🏥 Моніторинг здоров'я запускається (connect.go)
├─ 📦 Провайдер даних ініціалізується (db_provider.go)
└─ 🎨 UI компоненти побудовуються (main.go)
```

---

## Де шукати логи?

### 📁 Файлові логи
- **`./log/app.log`** — основний лог програми
- **`./log/error.log`** — помилки окремо
- **Можливі архіви**: `app.log.1.gz`, `app.log.2.gz` тощо

### 💻 Консоль
При запуску програма виводить логи прямо в консоль

---

## 🔴 Критичні помилки, на які звернути увагу

| Помилка | Причина | Дія |
|---------|---------|-----|
| `❌ КРИТИЧНА ПОМИЛКА: Не вдалося налаштувати драйвер Firebird` | Драйвер не встановлений | Встановити Firebird SDK |
| `❌ Не вдалося створити директорію логів` | Проблема з доступом | Перевірити права доступу до `./log` |
| `⚠️  БД недоступна при старті` | Firebird не запущений | Запустити Firebird сервер |
| `❌ БАГАТОРАЗОВІ ВІДМОВИ З'єднання з БД` | Нестійке з'єднання | Перевірити мережу та firewall |

---

## 📊 Рівні логування в файлі

```
DEBUG   ← Детальна інформація (можна вимкнути для продакшену)
INFO    ← Важливі моменти (завжди активний)
WARN    ← Попередження про проблеми
ERROR   ← Помилки
```

### Як змінити рівень логування?

У файлі `pkg/logger/logger.go` змініть:
```go
func DefaultConfig() *Config {
    return &Config{
        LogLevel: "debug",  // Змініть на "info", "warn", "error"
        // ...
    }
}
```

---

## 🎯 Популярні сценарії

### 1️⃣ Програма не запускається — як діагностувати?

**Дивіться на логи в цьому порядку:**
```
1. ЗАПУСК → З'єднання з БД встановлено? ✅ або ❌
2. Якщо ❌ → перевірте connect.go логи
3. Якщо ✅ → чи завантажилися UI компоненти?
```

### 2️⃣ БД часто відпадає

**Ищите у `app.log`:**
```
Рядок 1: ⚠️  Втрачено зв'язок з Firebird
Рядок 2: ⚠️  Втрачено зв'язок з Firebird
Рядок 3: ❌ БАГАТОРАЗОВІ ВІДМОВИ

→ Проблема! Перевірте Firebird сервер та мережу
```

### 3️⃣ Дані не завантажуються

**Ищите:**
```
[DEBUG] 📦 Завантаження списку об'єктів з БД...
❌ [ERROR] Помилка завантаження списку об'єктів

→ Проблема з запитом до БД або правами доступу
```

### 4️⃣ Тривога не оновлюється на екрані

**Ищите:**
```
[DEBUG] 🚨 Завантаження активних тривог з БД...
[INFO] ⚠️  АКТИВНІ ТРИВОГИ ЗНАЙДЕНО! count=3
[DEBUG] ✓ Тривоги завантажено

→ Тривоги є в БД, перевірте UI оновлення
```

---

## 💡 Приклади реальних логів

### Успішний старт:
```
✅ Система логування готова
║   ЗАПУСК ПРОГРАМИ - АРМ ПОЖЕЖНОЇ БЕЗПЕКИ v1.0
✅ З'єднання з БД встановлено успішно
✅ ІНІЦІАЛІЗАЦІЯ ЗАВЕРШЕНА. Програма готова до роботи.
```

### Проблема з БД:
```
❌ КРИТИЧНА ПОМИЛКА: Не вдалося налаштувати драйвер Firebird
error=connection refused
```

### Багаторазові відмови:
```
⚠️  Втрачено зв'язок з Firebird! failCount=1
⚠️  Втрачено зв'язок з Firebird! failCount=2
❌ БАГАТОРАЗОВІ ВІДМОВИ З'єднання з БД! consecutiveFailures=3
```

---

## 🔧 Налаштування логування

### Збільшити кількість логів (DEBUG)
```go
// У logger/logger.go
LogLevel: "debug",
```

### Вимкнути консолень лог
```go
// У logger/logger.go
EnableConsole: false,
```

### Збільшити розмір файлу логу
```go
// У logger/logger.go
MaxSize: 200,  // 200 MB замість 100 MB
```

---

## 🎓 Як читати логи?

```
| LEVEL | TIME     | MESSAGE                        | FIELD1=value1 FIELD2=value2
| INFO  | 15:04:05 | З'єднання з БД встановлено     | 
| DEBUG | 15:04:06 | Деталі операції                | objectID=15 name="Магазин ABC"
| WARN  | 15:04:07 | Потенційна проблема            | error=timeout
| ERROR | 15:04:08 | Помилка!                       | err=connection refused
```

### Розшифровка:
- **LEVEL** — серйозність повідомлення
- **TIME** — час повідомлення
- **MESSAGE** — основна інформація
- **FIELD=value** — додаткові контексті дані

---

## 📞 Корисні команди

### Переглід останніх 50 рядків логу:
```bash
tail -50 ./log/app.log
```

### Пошук за ключовим словом:
```bash
grep "ERROR" ./log/app.log
grep "Тривога" ./log/app.log
grep "З'єднання" ./log/app.log
```

### Встати з помилками:
```bash
cat ./log/error.log
```

### Переглід в реальному часі:
```bash
tail -f ./log/app.log
```

---

## ✅ Чеклист для запуску

- [ ] Firebird сервер запущений
- [ ] Мережа працює без проблем
- [ ] Користувач БД має права доступу
- [ ] Директорія `./log/` існує
- [ ] Немає помилок на етапі 1-3 запуску

---

**Останнє оновлення: 05.02.2026**
